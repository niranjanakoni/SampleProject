name: test CI

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.after }}
